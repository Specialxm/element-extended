import { describe, it, expect } from 'vitest'

// import { mount } from '@vue/test-utils'
// import HelloWorld from '../Corpper.vue'

describe('HelloWorld', () => {
  it('renders properly', () => {
    // const wrapper = mount(HelloWorld, { props: { url: 'Hello Vitest' } })
    // expect(wrapper.text()).toContain('Hello Vitest')
  })
})
